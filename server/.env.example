# Server environment variables

PORT=3847
DATA_DIR=./

# Moderation (required for admin API)
ADMIN_SECRET=change-me-to-a-long-random-string

# Report notifications (logged to console; email optional)
REPORT_EMAIL=office@tgbc.co.il

# Optional SMTP (TODO — wire in server/src/notify.js when ready)
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=office@tgbc.co.il
